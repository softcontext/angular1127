import { SortingCompaniesPipe } from './sorting-companies.pipe';

describe('SortingCompaniesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingCompaniesPipe();
    expect(pipe).toBeTruthy();
  });
});
